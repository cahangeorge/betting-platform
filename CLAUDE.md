# Bet Agent Guide

Use `AGENTS.md` as the source of truth for project instructions.

Current platform work targets `frontend/` (SvelteKit/Svelte 5) and `backend/` (FastAPI). Preserve dirty worktree changes, respect submodule boundaries, and run commands from the relevant sub-project directory.
