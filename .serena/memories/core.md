# Bet Workspace Memory Root

- Current implementation checkpoint, verified gates, migration head, dirty-tree
  cautions, and new-session commands: `mem:platform_hardening`.
- Memory-writing conventions and graph maintenance: `mem:memory_maintenance`.
